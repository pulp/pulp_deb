Added ``version``, ``origin``, ``label``, and ``description`` fields to Releases. These fields can
be set when creating new Releases via the API or when syncing if the Remote's
``sync_extra_release_fields`` option is set to true.
