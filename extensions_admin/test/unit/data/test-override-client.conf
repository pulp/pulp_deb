[output]
poll_frequency_in_seconds = .5

[logging]

filename = ~/.pulp/test_admin.log

# If present, the raw REST reponses will be logged to the given file
call_log_filename = ~/.pulp/test_server_calls.log
